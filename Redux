1. Add redux and react-redux via yarn or npm
2. Import provider and store in index.js form react-redux and redux/store respectively.
3. Wrap the component with Provider and pass store as a prop to provider.